---
name: typescript
description: Use for all coding tasks to ensure type safety and consistency.
---

# TypeScript Skill

## Directives
- **Zero Any**: Use `unknown` or specific interfaces instead of `any`.
- **Interfaces**: Define all database-related types (Recipe, Ingredient, Tag) in `src/types/database.ts`.
- **Prop Typing**: Always use strict types for component props.
